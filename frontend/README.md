# ADP-Themed Payroll — Frontend

Vite + React single-page app with an ADP-inspired theme.

## Quick start
```bash
cd frontend
npm install
npm run dev
```

By default the app talks to `http://localhost:8080/api`. Override via `.env`:

```
VITE_API_BASE_URL=http://localhost:8080/api
```
